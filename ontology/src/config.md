---
main_ontology: ontology/src/status.ttl
reasoner: hermit
---

This template relies on OntoQA reusable workflows.

Update `main_ontology` to point at your ontology entrypoint file (relative to the repository root).
You can also change `reasoner` (e.g. `hermit`, `pellet`, `elk`, `jfact`, `structural`).
